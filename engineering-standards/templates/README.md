<!--
Non-exclusive README hints for AI-agent-driven projects.

This is NOT a full README template to drop in wholesale. It is a short list of optional
sections worth adding to a project's EXISTING README.md, for human readers (not agents —
agents already read AGENTS.md every session). Copy only the parts that fit. Delete the rest,
including this comment block, once you're done.
-->

## For contributors (optional)

<!-- One or two lines near the top of the README, pointing humans at the agent rules. -->

This repo uses `AGENTS.md` to govern AI coding agent behavior — session-start checks, the
`task-plans` workflow, and project-specific rules. Read it before making changes with an agent.

## Status badge (optional)

<!-- e.g. `![status](https://img.shields.io/badge/status-early--poc-orange)` — only if the
project's maturity isn't obvious from context, and only one badge; avoid badge walls. -->

## Quick start (optional)

<!-- Only if the project has non-obvious setup steps beyond `pnpm install`. Link to a fuller
docs/ page instead of duplicating install/build/test instructions that already live in
AGENTS.md's Commands section. -->

## Structure (optional)

<!-- Only for repos with many files or non-obvious naming — a short top-level directory list,
not a full tree. Skip for small or self-explanatory repos; AGENTS.md's Project Overview may
already cover this for agents. -->
