---
name: execution-plans-workflow
description: Mechanics for the execution-plans/ task-artifact system - folder lifecycle, naming convention, and required file structure. Use when starting a non-trivial task, writing a plan, moving an artifact between todo/doing/done, recording an outcome, checking doing/ for unfinished work at the start of a session, or filing a note into the backlog.
---

# Execution-Plans Workflow

Non-trivial tasks get one Markdown artifact under `execution-plans/`. This skill covers the mechanics. The session-level rules — when to create an artifact at all, proactive filing, and the "check `doing/` first" rule — live in the project's `AGENTS.md`.

Read this before creating, moving, or completing an artifact.

## Before you start: two things come from the project

This skill is shared across repositories and is intentionally project-agnostic. Two things vary per project. Read both from the project's `AGENTS.md` — never assume them:

- **Category names.** `AGENTS.md` states which categories the project uses. Common sets: `features` / `fixes` / `analysis` for code projects, `drafting` / `revisions` / `analysis` for documentation projects.
- **Which companion instruction files exist.** The files referenced at the end of this skill apply only if they are present in the project's `agents-instructions/` directory. Skip any that are not.

**Do not edit this file to record project-specific details.** It is installed by `npx skills` and is overwritten wholesale on update. Project configuration belongs in `AGENTS.md`.

## Directory structure and lifecycle

The directory is structured around a kanban-style lifecycle, categorized by the type of work:

`execution-plans/[status]/[category]/`

- **[status]**: `todo/`, `doing/`, `done/`
- **[category]**: the categories the project defines in `AGENTS.md`. Add another only when none of them fit.

**`todo/` scope rules:**

- `todo/[category]/` — Items to work on during the **current project phase**.
- `todo/deferred/` — Items intentionally deferred to a **future phase**. Not picked up until the project advances to that phase. Files here are not categorized into subfolders.

**Workflow rules:**

> The status folders track **active work**, not the Markdown document. `doing/` = the work is actively being done — writing code, editing content. `done/` = the work is implemented, verified, and committed.

1. **New request / planning**: Create (or keep) the Markdown file in `todo/[category]/`. Write plans, checklists, and context here. A fully-planned item stays in `todo/` until the work begins.
2. **Starting work**: Move the file to `doing/[category]/` when you begin.
3. **Completion**: Once the work is fully done, verified, and committed, move the file to `done/[category]/`.

## Stale work

If you find items in `doing/` that appear outdated or abandoned, move them back to `todo/` (preserving all notes) or forward to `done/` with an `## Outcome: Canceled — [reason]` note. Ask the user if unsure.

## Naming convention

Use this format: `YYYY-MM-DD-HHmm-[title].md`, e.g. `2026-03-01-1430-fix-auth-redirect.md`. Always write dates with time in 24-hour format (e.g. `2026-03-24 14:30`).

Keep the original timestamp in the filename when moving a file between status folders — it records when the task was opened, not when it moved.

One file per task. Plans, progress, and outcomes all live inside the same file using the structure below.

## Artifact file structure

Every artifact file must follow this layout:

```markdown
---
created: YYYY-MM-DD HH:mm
created_by: [LLM name and version]
edits:
  - date: YYYY-MM-DD HH:mm
    author: [LLM name and version]
---

## Context

[Concisely: why this task exists]

## Plan

- [ ] Step 1
- [ ] Step 2

...

## Rationale

_If needed — for reviewers_

[Why this approach over alternatives, key findings from investigation, design decisions, constraints, assumptions to verify]

---

## Outcome

[What was done and why — keep it concise]

### Notes

_If needed_

[Trade-offs, deferred work, follow-ups]
```

- Fill `Context` and `Plan` while the file is in `todo/`.
- Fill `Rationale` while planning when the approach isn't self-evident, or when it rests on significant or deep investigation whose findings a reviewer would otherwise overlook — record what led to the decision so reviewers don't miss it. Skip it for trivial work.
- Fill `Outcome` when moving to `done/`. Check off plan items as you go.

## Companion instruction files

Apply each of these only if the file exists in the project's `agents-instructions/` directory:

- **`implementation-checklist.md`** — for implementation work, include its checklist in your plan.
- **`prompt-authoring-guide.md`** — follow it when writing or reviewing prompts for major work.
- **`post-implementation-checklist.md`** — follow it after completing work.
